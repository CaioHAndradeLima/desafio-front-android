package com.test.designsystem;

import android.content.Context;
import android.util.AttributeSet;
import com.gizmin.bitstore.custom_view.custom_loading.LoadingDots;

public class CustomLoading extends LoadingDots {
    public CustomLoading(Context context) {
        super(context);
    }

    public CustomLoading(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public CustomLoading(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public CustomLoading(Context context, AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}

